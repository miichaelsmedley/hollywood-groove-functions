{
  "name": "hollywood-groove-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for Hollywood Groove scoring and engagement",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions --config ../firebase.json --project theta-inkwell-448908-g9",
    "shell": "npm run build && firebase functions:shell --config ../firebase.json --project theta-inkwell-448908-g9",
    "start": "npm run shell",
    "deploy": "npm run build && firebase deploy --only functions --config ../firebase.json --project theta-inkwell-448908-g9",
    "logs": "firebase functions:log --config ../firebase.json --project theta-inkwell-448908-g9"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.19.27",
    "typescript": "^5.3.0"
  },
  "private": true
}
